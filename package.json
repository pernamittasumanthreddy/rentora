{
  "name": "rentora-platform",
  "version": "1.0.0",
  "description": "Rentora - Smart Product, Vehicle & Hardware Rental Platform",
  "main": "app.py",
  "scripts": {
    "start": "python app.py",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && ./mvnw spring-boot:run\"",
    "build": "npm run build --prefix frontend && cd backend && mvn clean package -DskipTests",
    "test": "pytest tests/ -v",
    "test:coverage": "pytest tests/ --cov=services --cov=backend --cov-report=term-missing",
    "lint": "eslint frontend/src --ext .js,.jsx",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "keywords": [
    "rental-marketplace",
    "escrow-protection",
    "vehicle-rental",
    "hardware-sharing",
    "iot-telematics",
    "full-stack"
  ],
  "author": "Rentora Engineering Team",
  "private": true,
  "license": "Proprietary",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
