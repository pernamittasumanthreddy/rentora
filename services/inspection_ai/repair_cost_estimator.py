"""
Automotive & Cinematic Gear Replacement Parts and Repair Cost Estimator
"""

def evaluate_replacement_part_1(wear_severity: str) -> dict:
    base_cost = 1150
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10001",
        "part_name": f"OEM Replacement Component #1",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_2(wear_severity: str) -> dict:
    base_cost = 1300
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10002",
        "part_name": f"OEM Replacement Component #2",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_3(wear_severity: str) -> dict:
    base_cost = 1450
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10003",
        "part_name": f"OEM Replacement Component #3",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_4(wear_severity: str) -> dict:
    base_cost = 1600
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10004",
        "part_name": f"OEM Replacement Component #4",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_5(wear_severity: str) -> dict:
    base_cost = 1750
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10005",
        "part_name": f"OEM Replacement Component #5",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_6(wear_severity: str) -> dict:
    base_cost = 1900
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10006",
        "part_name": f"OEM Replacement Component #6",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_7(wear_severity: str) -> dict:
    base_cost = 2050
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10007",
        "part_name": f"OEM Replacement Component #7",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_8(wear_severity: str) -> dict:
    base_cost = 2200
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10008",
        "part_name": f"OEM Replacement Component #8",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_9(wear_severity: str) -> dict:
    base_cost = 2350
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10009",
        "part_name": f"OEM Replacement Component #9",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_10(wear_severity: str) -> dict:
    base_cost = 2500
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10010",
        "part_name": f"OEM Replacement Component #10",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_11(wear_severity: str) -> dict:
    base_cost = 2650
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10011",
        "part_name": f"OEM Replacement Component #11",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_12(wear_severity: str) -> dict:
    base_cost = 2800
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10012",
        "part_name": f"OEM Replacement Component #12",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_13(wear_severity: str) -> dict:
    base_cost = 2950
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10013",
        "part_name": f"OEM Replacement Component #13",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_14(wear_severity: str) -> dict:
    base_cost = 3100
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10014",
        "part_name": f"OEM Replacement Component #14",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_15(wear_severity: str) -> dict:
    base_cost = 3250
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10015",
        "part_name": f"OEM Replacement Component #15",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_16(wear_severity: str) -> dict:
    base_cost = 3400
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10016",
        "part_name": f"OEM Replacement Component #16",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_17(wear_severity: str) -> dict:
    base_cost = 3550
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10017",
        "part_name": f"OEM Replacement Component #17",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_18(wear_severity: str) -> dict:
    base_cost = 3700
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10018",
        "part_name": f"OEM Replacement Component #18",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_19(wear_severity: str) -> dict:
    base_cost = 3850
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10019",
        "part_name": f"OEM Replacement Component #19",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_20(wear_severity: str) -> dict:
    base_cost = 4000
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10020",
        "part_name": f"OEM Replacement Component #20",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_21(wear_severity: str) -> dict:
    base_cost = 4150
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10021",
        "part_name": f"OEM Replacement Component #21",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_22(wear_severity: str) -> dict:
    base_cost = 4300
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10022",
        "part_name": f"OEM Replacement Component #22",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_23(wear_severity: str) -> dict:
    base_cost = 4450
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10023",
        "part_name": f"OEM Replacement Component #23",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_24(wear_severity: str) -> dict:
    base_cost = 4600
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10024",
        "part_name": f"OEM Replacement Component #24",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_25(wear_severity: str) -> dict:
    base_cost = 4750
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10025",
        "part_name": f"OEM Replacement Component #25",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_26(wear_severity: str) -> dict:
    base_cost = 4900
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10026",
        "part_name": f"OEM Replacement Component #26",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_27(wear_severity: str) -> dict:
    base_cost = 5050
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10027",
        "part_name": f"OEM Replacement Component #27",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_28(wear_severity: str) -> dict:
    base_cost = 5200
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10028",
        "part_name": f"OEM Replacement Component #28",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_29(wear_severity: str) -> dict:
    base_cost = 5350
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10029",
        "part_name": f"OEM Replacement Component #29",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_30(wear_severity: str) -> dict:
    base_cost = 5500
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10030",
        "part_name": f"OEM Replacement Component #30",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_31(wear_severity: str) -> dict:
    base_cost = 5650
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10031",
        "part_name": f"OEM Replacement Component #31",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_32(wear_severity: str) -> dict:
    base_cost = 5800
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10032",
        "part_name": f"OEM Replacement Component #32",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_33(wear_severity: str) -> dict:
    base_cost = 5950
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10033",
        "part_name": f"OEM Replacement Component #33",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_34(wear_severity: str) -> dict:
    base_cost = 6100
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10034",
        "part_name": f"OEM Replacement Component #34",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_35(wear_severity: str) -> dict:
    base_cost = 6250
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10035",
        "part_name": f"OEM Replacement Component #35",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_36(wear_severity: str) -> dict:
    base_cost = 6400
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10036",
        "part_name": f"OEM Replacement Component #36",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_37(wear_severity: str) -> dict:
    base_cost = 6550
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10037",
        "part_name": f"OEM Replacement Component #37",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_38(wear_severity: str) -> dict:
    base_cost = 6700
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10038",
        "part_name": f"OEM Replacement Component #38",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_39(wear_severity: str) -> dict:
    base_cost = 6850
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10039",
        "part_name": f"OEM Replacement Component #39",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_40(wear_severity: str) -> dict:
    base_cost = 7000
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10040",
        "part_name": f"OEM Replacement Component #40",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_41(wear_severity: str) -> dict:
    base_cost = 7150
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10041",
        "part_name": f"OEM Replacement Component #41",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_42(wear_severity: str) -> dict:
    base_cost = 7300
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10042",
        "part_name": f"OEM Replacement Component #42",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_43(wear_severity: str) -> dict:
    base_cost = 7450
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10043",
        "part_name": f"OEM Replacement Component #43",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_44(wear_severity: str) -> dict:
    base_cost = 7600
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10044",
        "part_name": f"OEM Replacement Component #44",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_45(wear_severity: str) -> dict:
    base_cost = 7750
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10045",
        "part_name": f"OEM Replacement Component #45",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_46(wear_severity: str) -> dict:
    base_cost = 7900
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10046",
        "part_name": f"OEM Replacement Component #46",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_47(wear_severity: str) -> dict:
    base_cost = 8050
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10047",
        "part_name": f"OEM Replacement Component #47",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_48(wear_severity: str) -> dict:
    base_cost = 8200
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10048",
        "part_name": f"OEM Replacement Component #48",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_49(wear_severity: str) -> dict:
    base_cost = 8350
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10049",
        "part_name": f"OEM Replacement Component #49",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_50(wear_severity: str) -> dict:
    base_cost = 8500
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10050",
        "part_name": f"OEM Replacement Component #50",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_51(wear_severity: str) -> dict:
    base_cost = 8650
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10051",
        "part_name": f"OEM Replacement Component #51",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_52(wear_severity: str) -> dict:
    base_cost = 8800
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10052",
        "part_name": f"OEM Replacement Component #52",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_53(wear_severity: str) -> dict:
    base_cost = 8950
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10053",
        "part_name": f"OEM Replacement Component #53",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_54(wear_severity: str) -> dict:
    base_cost = 9100
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10054",
        "part_name": f"OEM Replacement Component #54",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_55(wear_severity: str) -> dict:
    base_cost = 9250
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10055",
        "part_name": f"OEM Replacement Component #55",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_56(wear_severity: str) -> dict:
    base_cost = 9400
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10056",
        "part_name": f"OEM Replacement Component #56",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_57(wear_severity: str) -> dict:
    base_cost = 9550
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10057",
        "part_name": f"OEM Replacement Component #57",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_58(wear_severity: str) -> dict:
    base_cost = 9700
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10058",
        "part_name": f"OEM Replacement Component #58",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_59(wear_severity: str) -> dict:
    base_cost = 9850
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10059",
        "part_name": f"OEM Replacement Component #59",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_60(wear_severity: str) -> dict:
    base_cost = 10000
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10060",
        "part_name": f"OEM Replacement Component #60",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_61(wear_severity: str) -> dict:
    base_cost = 10150
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10061",
        "part_name": f"OEM Replacement Component #61",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_62(wear_severity: str) -> dict:
    base_cost = 10300
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10062",
        "part_name": f"OEM Replacement Component #62",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_63(wear_severity: str) -> dict:
    base_cost = 10450
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10063",
        "part_name": f"OEM Replacement Component #63",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_64(wear_severity: str) -> dict:
    base_cost = 10600
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10064",
        "part_name": f"OEM Replacement Component #64",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_65(wear_severity: str) -> dict:
    base_cost = 10750
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10065",
        "part_name": f"OEM Replacement Component #65",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_66(wear_severity: str) -> dict:
    base_cost = 10900
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10066",
        "part_name": f"OEM Replacement Component #66",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_67(wear_severity: str) -> dict:
    base_cost = 11050
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10067",
        "part_name": f"OEM Replacement Component #67",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_68(wear_severity: str) -> dict:
    base_cost = 11200
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10068",
        "part_name": f"OEM Replacement Component #68",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_69(wear_severity: str) -> dict:
    base_cost = 11350
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10069",
        "part_name": f"OEM Replacement Component #69",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_70(wear_severity: str) -> dict:
    base_cost = 11500
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10070",
        "part_name": f"OEM Replacement Component #70",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_71(wear_severity: str) -> dict:
    base_cost = 11650
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10071",
        "part_name": f"OEM Replacement Component #71",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_72(wear_severity: str) -> dict:
    base_cost = 11800
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10072",
        "part_name": f"OEM Replacement Component #72",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_73(wear_severity: str) -> dict:
    base_cost = 11950
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10073",
        "part_name": f"OEM Replacement Component #73",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_74(wear_severity: str) -> dict:
    base_cost = 12100
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10074",
        "part_name": f"OEM Replacement Component #74",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_75(wear_severity: str) -> dict:
    base_cost = 12250
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10075",
        "part_name": f"OEM Replacement Component #75",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_76(wear_severity: str) -> dict:
    base_cost = 12400
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10076",
        "part_name": f"OEM Replacement Component #76",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_77(wear_severity: str) -> dict:
    base_cost = 12550
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10077",
        "part_name": f"OEM Replacement Component #77",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_78(wear_severity: str) -> dict:
    base_cost = 12700
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10078",
        "part_name": f"OEM Replacement Component #78",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_79(wear_severity: str) -> dict:
    base_cost = 12850
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10079",
        "part_name": f"OEM Replacement Component #79",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_80(wear_severity: str) -> dict:
    base_cost = 13000
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10080",
        "part_name": f"OEM Replacement Component #80",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_81(wear_severity: str) -> dict:
    base_cost = 13150
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10081",
        "part_name": f"OEM Replacement Component #81",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_82(wear_severity: str) -> dict:
    base_cost = 13300
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10082",
        "part_name": f"OEM Replacement Component #82",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_83(wear_severity: str) -> dict:
    base_cost = 13450
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10083",
        "part_name": f"OEM Replacement Component #83",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_84(wear_severity: str) -> dict:
    base_cost = 13600
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10084",
        "part_name": f"OEM Replacement Component #84",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_85(wear_severity: str) -> dict:
    base_cost = 13750
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10085",
        "part_name": f"OEM Replacement Component #85",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_86(wear_severity: str) -> dict:
    base_cost = 13900
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10086",
        "part_name": f"OEM Replacement Component #86",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_87(wear_severity: str) -> dict:
    base_cost = 14050
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10087",
        "part_name": f"OEM Replacement Component #87",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_88(wear_severity: str) -> dict:
    base_cost = 14200
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10088",
        "part_name": f"OEM Replacement Component #88",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_89(wear_severity: str) -> dict:
    base_cost = 14350
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10089",
        "part_name": f"OEM Replacement Component #89",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_90(wear_severity: str) -> dict:
    base_cost = 14500
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10090",
        "part_name": f"OEM Replacement Component #90",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_91(wear_severity: str) -> dict:
    base_cost = 14650
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10091",
        "part_name": f"OEM Replacement Component #91",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_92(wear_severity: str) -> dict:
    base_cost = 14800
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10092",
        "part_name": f"OEM Replacement Component #92",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_93(wear_severity: str) -> dict:
    base_cost = 14950
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10093",
        "part_name": f"OEM Replacement Component #93",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_94(wear_severity: str) -> dict:
    base_cost = 15100
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10094",
        "part_name": f"OEM Replacement Component #94",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_95(wear_severity: str) -> dict:
    base_cost = 15250
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10095",
        "part_name": f"OEM Replacement Component #95",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_96(wear_severity: str) -> dict:
    base_cost = 15400
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10096",
        "part_name": f"OEM Replacement Component #96",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_97(wear_severity: str) -> dict:
    base_cost = 15550
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10097",
        "part_name": f"OEM Replacement Component #97",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_98(wear_severity: str) -> dict:
    base_cost = 15700
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10098",
        "part_name": f"OEM Replacement Component #98",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.0,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_99(wear_severity: str) -> dict:
    base_cost = 15850
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10099",
        "part_name": f"OEM Replacement Component #99",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 2.5,
        "availability": "IN_STOCK"
    }

def evaluate_replacement_part_100(wear_severity: str) -> dict:
    base_cost = 16000
    multiplier = 1.5 if wear_severity == "SEVERE" else 1.0
    return {
        "part_id": f"PART-RENT-10100",
        "part_name": f"OEM Replacement Component #100",
        "estimated_cost_inr": round(base_cost * multiplier, 2),
        "labor_hours": 1.0,
        "availability": "IN_STOCK"
    }

def calculate_total_repair_quote(part_indices: list, severity: str = "MODERATE") -> float:
    total = 0.0
    for idx in part_indices:
        func = globals().get("evaluate_replacement_part_" + str(idx))
        if func:
            total += func(severity)["estimated_cost_inr"]
    return total
